# 📊 Web App Nomes

Este projeto é um aplicativo web interativo que consulta a popularidade de nomes ao longo das décadas usando a API do IBGE. Desenvolvido com **Streamlit**, ele permite visualizar a frequência dos nomes em diferentes períodos por meio de tabelas e gráficos dinâmicos.

## 🚀 Tecnologias Utilizadas
- **Python** → Linguagem principal do projeto
- **Streamlit** → Framework para criação do app web
- **Requests** → Para consumir a API do IBGE
- **Pandas** → Para manipulação dos dados

## 🔍 Como Funciona?
1. O usuário insere um nome no campo de pesquisa.
2. O aplicativo consulta a API do IBGE e retorna a frequência do nome por década.
3. Os dados são exibidos em uma tabela e um gráfico de linha.

## 📦 Como Executar o Projeto
1. Clone o repositório ou baixe o código.
2. Instale as dependências:
   ```sh
   pip install streamlit pandas requests
   ```
3. Execute o app com o comando:
   ```sh
   streamlit run app.py
   ```

## 📌 Créditos
**Feito por Gabriel Rangel**

# miniprojeto_web_nomes
